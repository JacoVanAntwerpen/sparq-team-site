---
import { COLLABORATORS } from '@data/collaborators';
---
<div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4 items-center">
  {COLLABORATORS.map(c => (
    <div class="flex items-center justify-center rounded-xl border border-neutral-200 bg-white h-16 text-neutral-600 font-semibold">{c}</div>
  ))}
</div>
